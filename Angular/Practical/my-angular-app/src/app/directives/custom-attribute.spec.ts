import { CustomAttribute } from './custom-attribute';

describe('CustomAttribute', () => {
  it('should create an instance', () => {
    const directive = new CustomAttribute();
    expect(directive).toBeTruthy();
  });
});
